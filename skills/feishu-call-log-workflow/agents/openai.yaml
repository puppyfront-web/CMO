interface:
  display_name: "Feishu Call Log Workflow"
  short_description: "Analyze call audio, create a pure mind-map doc, and append one row into Feishu Sheets."
  default_prompt: "Use $feishu-call-log-workflow to analyze this call recording and write the result into my Feishu call log sheet."

policy:
  allow_implicit_invocation: true
